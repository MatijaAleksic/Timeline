export default class ApiRoutes {
  public static HOME: string = "/api";
  public static EVENTS: string = this.HOME + "/events";
  public static PERIODS: string = this.HOME + "/periods";
  public static TIMELINE: string = this.HOME + "/timeline";
}
